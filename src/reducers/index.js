"use strict";
import { combineReducers } from "redux";
var query = (state = {}, action = {})=>{
    switch (action.type){
        default:
            return state;
    }
};
export default combineReducers({ query });

